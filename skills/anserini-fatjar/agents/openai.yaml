interface:
  display_name: "Anserini Fatjar"
  short_description: "Install and run Anserini from the latest Maven fatjar"
  default_prompt: "Use $anserini-fatjar to verify JDK 21 and wget first, warn if prerequisites are not satisfied, then download the latest Anserini fatjar from Maven Central and run the CACM smoke test."
